# On CI, the database is reached through the container network instead of a published host port
# (see docker/docker-compose-ci.yml).
spring:
  datasource:
    url: "jdbc:postgresql://jme-bptest-order-testagent-db-local:5432/jme-bptest-order-testagent-db-local"
